const form=document.getElementById('login-form');const error=document.getElementById('error');const username=document.getElementById('username');document.querySelectorAll('.user').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.user').forEach(x=>x.classList.remove('active'));btn.classList.add('active');username.value=btn.dataset.user;error.style.display='none';document.getElementById('password').focus()}));form?.addEventListener('submit',async e=>{e.preventDefault();error.style.display='none';const user=username.value;const password=document.getElementById('password').value;try{const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({user,password}),credentials:'include'});const data=await r.json().catch(()=>({}));if(!r.ok||!data.ok)throw new Error(data.error||'Login gagal');window.location.replace(data.redirect); }catch(err){console.error('login:',err);error.textContent=err.message||'Login gagal.';error.style.display='block'}});